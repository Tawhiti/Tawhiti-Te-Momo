# Netlify build + hosting config for the Tawhiti Media site.
[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: serve index.html for any route the browser requests.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
